export default function DetermineBorrowStatus(clientAPI) {

  var status =  `{Status}`;
 // if ( status === '1' ) {
  //    return  'Overdue';
  // } else { 
  //  return  'OK';
  // }
  if ( `{Status}` === `1` ) {
      return  'Overdue';
   }
}